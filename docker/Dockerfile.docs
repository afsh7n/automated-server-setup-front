# Dockerfile for Onomis Docs
FROM node:16-alpine

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run docs:build

RUN npm install -g serve
EXPOSE 3001

CMD ["serve", "-s", ".vitepress/dist"]
