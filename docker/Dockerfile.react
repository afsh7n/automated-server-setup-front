# Dockerfile for React Project
FROM node:16-alpine

WORKDIR /usr/src/app

COPY ./../src/onomis-react/package*.json ./

RUN npm install

COPY ./../src/onomis-react/. .

RUN npm run build

RUN npm install -g serve

EXPOSE 3000

CMD ["serve", "-s", "dist", "-l", "3000"]
